table:
  name: network
  schema: public
select_permissions:
  - permission:
      allow_aggregations: true
      columns:
        - name
        - type
        - params
      filter: {}
    role: anonymous
