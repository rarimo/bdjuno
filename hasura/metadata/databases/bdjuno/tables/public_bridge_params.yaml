table:
  name: bridge_params
  schema: public
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - one_row_id
    - withdraw_denom
    - height
    filter: {}
  role: anonymous
